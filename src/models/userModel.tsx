export interface RegisterloginModel{
    id: Number,
    token: String
}

export interface UserModel{
    avatar: string,
    id: Number,
    first_name: string,
    last_name: string,
    email: string
}